;;; windows.el --- Emacs windows settings -*- lexical-binding: t -*-

;;; Commentary:
;;

;; Split windows to the right by default
(setq split-height-threshold 80)
(setq split-width-threshold 160)

(provide 'windows)

;;; windows.el ends here
